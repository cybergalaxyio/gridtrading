# Branch workflow

- Use `dev` for ongoing development. Switch to `dev` before making changes.
- Use `main` for releases. When a release is requested, run the relevant checks and merge `dev` into `main`.
- Return to `dev` after completing a release so future work continues there.
