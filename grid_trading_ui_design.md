# Grid Trading Web 页面设计（低保真）

**状态：** 初步页面方案  
**更新日期：** 2026-08-24  
**目标终端：** 桌面浏览器优先，兼容平板；手机端以监控和紧急操作为主  
**对应技术方案：** [grid_trading_technical_stack.md](./grid_trading_technical_stack.md)

## 1. 设计目标

界面围绕“人工决定何时运行，后端负责运行期间自动下单”设计。

- 快速判断策略是否安全、是否正在运行，以及交易所状态是否正常。
- 开启、暂停、继续、关闭和紧急停止始终容易找到，但危险操作不能误触。
- 图表同时表达市场价格、网格结构、订单、成交和持仓风险。
- 后端未确认操作成功前，前端不得显示为成功。
- 模拟盘、测试网和实盘必须有明显且持续可见的环境标识。

## 2. 信息架构

```text
Grid Trading
├─ 控制台 Dashboard
│  ├─ 策略状态与控制
│  ├─ 实时交易图表
│  ├─ 账户、持仓与收益
│  └─ 当前订单、成交、日志和告警
├─ 策略 Strategies
│  ├─ 策略列表
│  ├─ 新建/编辑策略
│  └─ 历史运行记录
├─ 订单与成交 Orders
│  ├─ 当前挂单
│  ├─ 历史订单
│  └─ 成交记录
├─ 风险与告警 Risk
│  ├─ 当前风险状态
│  ├─ 风控事件
│  └─ 告警历史
└─ 系统设置 Settings
   ├─ 交易所账户
   ├─ 通知设置
   ├─ 显示与时区
   └─ 系统状态
```

MVP 将“控制台”作为默认首页，其余页面作为详细信息入口。

## 3. 全局框架

建议桌面端以 `1440 × 900` 为主要设计基准。

```text
┌──────────────────────────────────────────────────────────────────────────────┐
│ Grid Trading  [TESTNET]  Bybit · Connected     UTC 12:30:25    [紧急停止]  ● │
├──────────────┬───────────────────────────────────────────────────────────────┤
│  控制台       │                                                               │
│  策略         │                    当前页面内容                               │
│  订单与成交   │                                                               │
│  风险与告警   │                                                               │
│  系统设置     │                                                               │
│              │                                                               │
│ v0.1         │                                                               │
└──────────────┴───────────────────────────────────────────────────────────────┘
```

顶栏始终显示当前环境、交易所连接、行情延迟、时区和全局紧急停止。实盘环境使用明显的持续边框或背景标识，不能只依赖一小段文字区分。

左侧导航展开宽度约 `208px`，可折叠为图标栏；未处理的风险告警显示数量徽标。

## 4. 控制台页面

控制台是最重要的页面，用户应在一个屏幕内看到策略、市场、订单和风险状态。

### 4.1 桌面线框图

```text
┌────────────────────────────────────────────────────────────────────────────────────┐
│ BTCUSDT 永续       63,420.5  +0.82%       Strategy: Weekend Grid #1       [RUNNING] │
│ Bybit · Isolated · 1x             上次同步 2s 前        [暂停] [关闭 ▾] [编辑参数]  │
├─────────────────────────────────────────────────────────────┬──────────────────────┤
│  1m  5m  15m  1h  4h       指标 ▾       适应窗口           │  策略摘要             │
│ ┌─────────────────────────────────────────────────────────┐ │  区间 61,800–65,200   │
│ │                     SELL GRID                          ─ │ │  网格 14 层           │
│ │                    ───────── 64,800                      │ │  使用资金 5,000 USDT  │
│ │              ╭──── OHLC / Candlestick                   │ │  运行 06:21:34        │
│ │ BUY 63,250 ● ╯                    Current 63,420 ─────── │ ├──────────────────────┤
│ │              ╲                                          │ │  账户与持仓           │
│ │               ╲─────── 62,900                           │ │  净仓位 +0.012 BTC    │
│ │                     BUY GRID                           ─ │ │  均价 63,105          │
│ ├─────────────────────────────────────────────────────────┤ │  浮盈 +3.78 USDT      │
│ │                    Volume                               │ │  可用 8,420 USDT      │
│ └─────────────────────────────────────────────────────────┘ ├──────────────────────┤
│  图例：● 买入成交  ◆ 卖出成交  ─ 活跃订单  ┄ 计划网格      │  收益                 │
│                                                             │  净收益 +20.50 USDT    │
├─────────────────────────────────────────────────────────────┴──────────────────────┤
│ [当前挂单 12] [最近成交] [策略事件] [风险告警 1]                                  │
├──────────┬──────┬────────────┬────────────┬──────────┬─────────────┬──────────────┤
│ 时间     │ 方向 │ 价格       │ 数量       │ 状态     │ 网格层级    │ 操作         │
│ 12:28:01 │ BUY  │ 63,250.0   │ 0.002 BTC  │ Working  │ Buy L2      │ [撤单]       │
│ 12:27:54 │ SELL │ 63,750.0   │ 0.002 BTC  │ Working  │ Sell L1     │ [撤单]       │
└──────────┴──────┴────────────┴────────────┴──────────┴─────────────┴──────────────┘
```

### 4.2 策略状态与按钮

| 当前状态 | 主按钮 | 次要按钮 |
|---|---|---|
| `Stopped` | 开启 | 编辑参数 |
| `Starting` | 正在开启… | 取消（仅后端允许时） |
| `Running` | 暂停 | 关闭、编辑非关键参数 |
| `Pausing` | 正在暂停… | 无 |
| `Paused` | 继续 | 关闭、对账、编辑参数 |
| `Stopping` | 正在关闭… | 无 |
| `Faulted` | 查看原因 | 对账、关闭 |

按钮发送命令后进入等待状态，直到后端返回最终结果。超时显示“结果未知，正在对账”，不能简单恢复为原状态。

### 4.3 交易图表

图表约占主内容区宽度的 70%，默认展示：

- K 线和成交量。
- 当前运行区间及上下边界。
- 计划网格与已挂单网格使用不同线型。
- Buy/Sell 成交点使用不同形状并显示文字，不能只依赖红绿色。
- 当前价格、持仓均价和风控线。
- 鼠标悬停显示 OHLC、成交量和该价位订单信息。

```text
[✓] 计划网格  [✓] 活跃订单  [✓] 成交点  [✓] 持仓均价  [ ] 风控边界
```

第一版不在图上直接拖动网格下单。后续如支持拖动，松开鼠标后仍需弹出修改确认。

### 4.4 右侧摘要与底部面板

右侧固定展示：

1. **策略摘要：** 区间、网格层数、单格数量、使用资金和运行时长。
2. **账户与持仓：** 净仓位、持仓均价、可用余额、保证金和清算距离。
3. **收益：** 已实现网格利润、浮动盈亏、手续费、资金费和净收益。

底部通过 Tab 切换当前挂单、最近成交、策略事件和风险告警，支持按方向、状态和网格层级过滤。

## 5. 策略页面

### 5.1 策略列表

```text
┌────────────────────────────────────────────────────────────────────────────┐
│ 策略                                                   [+ 新建策略]        │
│ [全部] [运行中] [已暂停] [已停止]                   搜索策略或交易对       │
├────────────────────────────────────────────────────────────────────────────┤
│ Weekend Grid #1    BTCUSDT   RUNNING   净收益 +20.50   运行 6h   [打开]   │
│ ETH Range Grid     ETHUSDT   PAUSED    净收益  +8.20   暂停中     [打开]   │
│ BTC Test Grid      BTCUSDT   STOPPED   上次运行 Aug 20            [打开]   │
└────────────────────────────────────────────────────────────────────────────┘
```

每项显示名称、交易对、状态、运行环境、收益和风险告警。列表页不直接提供“开启”按钮，避免缺少上下文时误启动。

### 5.2 新建与编辑策略

```text
1 基本信息 ── 2 网格参数 ── 3 资金与风控 ── 4 预览确认
```

- **基本信息：** 策略名称、账户、环境、交易对、产品类型、保证金模式和杠杆。
- **网格参数：** 上下限、网格数、等差/等比、每格数量或总资金、Post-only。
- **资金与风控：** 最大资金、最大净持仓、最大挂单数、越界行为和失败阈值。
- **预览确认：** 参数摘要、预计网格图、所需资金、费用后单格收益和风险提示。

```text
┌────────────────────────────┬──────────────────────────────────────┐
│ 参数与风险摘要             │ 预计网格图                           │
│ 交易对 BTCUSDT             │ ─ 65,200                             │
│ 区间 61,800–65,200         │ ─ 64,950                             │
│ 14 层 / 5,000 USDT         │ ...                                  │
│ 最大仓位 0.08 BTC          │ ─ 61,800                             │
├────────────────────────────┴──────────────────────────────────────┤
│ [ ] 我已确认当前为 TESTNET，并理解策略会自动创建订单              │
│                                      [返回修改] [保存] [保存并开启] │
└───────────────────────────────────────────────────────────────────┘
```

实盘“保存并开启”必须二次确认，并展示交易所、账户、交易对、最大资金和最大仓位。

## 6. 订单与成交页面

```text
[当前挂单] [历史订单] [成交记录]
```

顶部提供时间范围、策略、交易对、方向、状态和订单 ID 筛选。订单详情通过右侧抽屉展示：

- 本地及交易所订单信息。
- 对应策略和网格层级。
- 状态变化时间线和部分成交明细。
- 失败错误码和原始响应。
- 当前允许执行的人工操作。

人工撤单必须显示执行中状态，并在交易所确认后更新为已撤销。

## 7. 风险与告警页面

```text
┌──────────────────┬──────────────────┬──────────────────┬──────────────────┐
│ Exchange         │ Market Data      │ Order Sync       │ Position Risk    │
│ Healthy          │ 220 ms           │ Synced 2s ago    │ 38% of limit     │
└──────────────────┴──────────────────┴──────────────────┴──────────────────┘
```

每条告警展示严重程度、发生/恢复时间、策略、交易对、原因、系统已采取的动作、人工处理建议和确认记录。

Critical 告警不能只用 Toast 表示，必须保留在页面和全局顶栏中，直到用户确认或系统恢复。

## 8. 系统设置页面

### 8.1 交易所账户

账户列表只显示账户别名、交易所、环境、API Key 掩码尾号、权限检查结果和最近连接测试时间。

Secret 保存后不再返回前端。新增或替换 Secret 使用独立表单，不提供明文查看功能。

### 8.2 系统状态

展示后端版本与启动时间、数据库状态、SignalR 状态、交易所 REST/WebSocket 状态、后台任务、服务器时间和时钟偏差。

## 9. 关键交互

### 9.1 开启

```text
点击开启
  ↓
检查交易所连接、余额权限、未知订单持仓、网格参数和风险限制
  ↓ 全部通过
用户最终确认
  ↓
Starting → 下单及对账 → Running
```

任一检查失败时不允许开启，并直接链接到具体问题。

### 9.2 暂停

```text
暂停策略

系统将停止创建新的入场单和补挂单。
现有挂单：保留
现有仓位：保持不变

[取消] [确认暂停]
```

### 9.3 关闭

```text
关闭策略

(●) 撤销策略挂单，保留当前仓位
( ) 撤销策略挂单，并提交平仓计划

当前挂单：12
当前净仓位：+0.012 BTC

[取消] [确认关闭]
```

“撤单并平仓”属于后续能力；未实现前不得显示该选项。

### 9.4 紧急停止

- 固定在顶栏，但与普通按钮保持足够间距。
- 点击后明确确认，不能单击即执行。
- 确认框展示受影响的策略、挂单和仓位。
- 默认撤单但不自动平仓，除非后续明确实现其他安全处置方案。

## 10. 视觉规范建议

交易控制台默认使用深色主题。

| 用途 | 建议颜色 |
|---|---|
| 页面背景 | `#0B0F14` |
| 卡片背景 | `#111820` |
| 边框 | `#26313D` |
| 主文字 | `#E6EDF3` |
| 次要文字 | `#8B98A5` |
| Buy / 正向 | `#22C55E` |
| Sell / 负向 | `#EF4444` |
| Warning | `#F59E0B` |
| Info / 主操作 | `#3B82F6` |

红色和绿色不能作为唯一信息载体；同时使用 `BUY`/`SELL` 文字、方向和不同形状。

| 状态 | 视觉建议 |
|---|---|
| Running | 绿色圆点 + `RUNNING` |
| Paused | 黄色暂停图标 + `PAUSED` |
| Starting / Stopping | 蓝色进度动画 + 状态文字 |
| Faulted | 红色错误图标 + `FAULTED` |
| Disconnected | 灰色断开图标 + 数据最后更新时间 |

价格和数量精度由交易所 symbol metadata 决定；盈亏注明币种，负数必须显示负号；相对时间可悬停查看完整 UTC 时间。

## 11. 响应式范围

- **桌面端：** 完整控制、图表、摘要和订单列表。
- **平板端：** 侧栏折叠，右侧摘要移至图表下方。
- **手机端：** 优先展示状态、风险、持仓和暂停/紧急停止；复杂编辑提示使用桌面端。

手机端不得隐藏实盘环境、连接异常或风险告警。

## 12. MVP 页面优先级

1. 控制台骨架、环境标识和连接状态。
2. K 线、网格线、订单及成交可视化。
3. 策略状态和开启、暂停、继续、关闭操作。
4. 策略配置与启动前检查。
5. 当前订单、成交和策略事件面板。
6. 风险告警和系统状态。
7. 策略历史、详细报表和移动端优化。

## 13. 尚待确认

- React UI 组件库。
- 前端状态管理和请求缓存方案。
- 第一阶段是否只允许同时运行一个策略。
- 暂停时现有挂单的最终默认处理方式。
- 是否在第一版支持从图表点击网格查看订单详情。
- 是否需要多账户和多交易所切换。
- 实盘开启是否要求输入策略名称或确认短语。

